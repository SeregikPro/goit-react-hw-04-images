import styled from 'styled-components';
import {
  space,
  color,
  typography,
  layout,
  flexbox,
  grid,
  border,
} from 'styled-system';

export const Box = styled('div')(
  space,
  color,
  typography,
  layout,
  flexbox,
  grid,
  border
);
